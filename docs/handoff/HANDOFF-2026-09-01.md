# 12Weeks — Handoff, 2026-09-01 (state at `9f5bc62`)

Read this first. `README.md` (2026-08-29) is the full system description and still accurate
except where this file says otherwise. `product-intent.md` is the canonical list of Erik's
binding decisions. `findings-status-2026-09-01.md` records every audit finding and its fix.

## 1. What happened today

79 commits on `main` (`e74c7e0..9f5bc62`), all deployed to Render, all in the main loop —
no workflows, no agents, no paid audits. Two halves:

**Morning — live incidents on Erik's card**
- Tesamorelin syringe correction: 10 mg/mL recon → 1 mg = **10u**, not 40u (all 46 rows, DB + CSV).
- BPC-157 doubled to 0.5 mg / 20u for plantar fasciitis, **Sep 1–28** (28 rows), reverts to 0.25 on Sep 29.
- Run card contradiction ("50 min" label over "45 min… trimmed… active celiac flare" prose):
  the flare was a **May 2026 coach memory** injected undated into the planner; the anti-regression
  rail restored the duration but not the prose. Fixed at every layer (memories dated; rail rewrites
  whole detail; evidence-only rule for the deload/reduce_running call; no deload wording on cards).
- Run not on the card: a blank `manual` RunLog row was blocking the Garmin autofill. Fixed + guarded.

**Rest of the day — the $7,000 audit's 165 findings, all closed**
5 criticals, 39 highs, 89 mediums, 32 lows. Verified against current code before each fix,
tested, deployed. Details per finding in `findings-status-2026-09-01.md`.

## 2. Things that did not exist yesterday (operate these)

| Thing | Where | Notes |
|---|---|---|
| Nightly Postgres backup | launchd `com.12weeks.db-backup` → `~/12weeks-backups/<date>.dump` + `.json` | PG18 client at `/opt/homebrew/opt/postgresql@18/bin`; DB URL in `~/.12weeks_db_url`. Restore: `pg_restore --no-owner --clean --if-exists -d <url> file.dump`. First backup verified 2026-09-01. |
| Two admin keys | `~/.12weeks_admin_read_key` (diagnostics), `~/.12weeks_admin_key` (writes) | Both on Render (`ADMIN_READ_KEY`, `ADMIN_API_KEY`). The old leaked literal is refused in code. Read routes: debug/sql, serve-as-user, today-status, full-day-state, show-sets, coach-context, version, admin/debug/user, admin/dashboard. |
| CI + pre-push gate | `.github/workflows/tests.yml`; `scripts/pre-push` (symlinked into `.git/hooks`) | Refuses a red suite. `pytest.ini`: RuntimeWarning is fatal; `tests/coach_audit` runs only with `--live-coach` (paid). |
| Protocol edit fast path v2 | `POST /api/admin/import-protocol?email=…` with `{"csv_text": …, "dry_run": true}` | Edit the CSV locally, dry-run to see the per-row diff, post again to apply. No deploy, no debug/exec. Then commit the CSV. |
| One-shot curve re-anchor | `POST /api/admin/block3-reanchor {email, dry_run}` | Rescales remaining weekly rates to still land on 195 at day 84. Exactly once per user. Run on a Monday morning. |
| Scale events | Weigh-in strip chips (Glutened / Sodium), `log_bodyweight(event=…)` tool, `[SCALE_EVENT]` marker | A recorded gluten event is authoritative for every pace surface. |
| Commitments | Settings → Commitments | Cadence-aware (weekly / every other week / once, anchor date). Both planners treat the day as fixed. |
| Coach marker log | `coach_marker_log` table; ⚠ flag bubble in chat; `<marker_outcomes>` section | A coach change that failed to persist is shown once and re-stated, never asserted. |
| Nightly compliance eval | 23:00–23:45 local in the push scheduler | Skipped lift / no weigh-in / unchecked dose → escalation events; clean day de-escalates. |
| Chase push | 09:30–10:30 local | Only when a weigh-in or a morning dose is still owed. |
| Vials + labs from the card | `/api/protocol/vials`, `/api/protocol/lab/<id>/complete` | No more curl. |
| Weekly progress photos | Sunday measurements form (Front / Side) | Same-pose comparison feeds the review opener. |
| Weekly verdict | `WeeklyReport.verdict` | ON_TRACK / SCALE_ONLY / LIFTS_ONLY / OFF — handed to the review agent as THE grade. |
| New modules | `llm_client.py` (models, client, JSON parser — three modules had stale model ids the API rejects), `program_calendar.py`, `meal_types.py`, `intake_profile.py`, `commitments.py`, `garmin_registry.py` | Single sources; tests refuse copies. |

## 3. Decisions Erik made today (binding)

- **Every movement auto-reconciles**, isolations included (`_should_autoreconcile` is `bool(exercise)`).
- **No "confirm with your doctor" gate**: a late dose is taken when noticed and logged; `/late` never
  refuses by age; the card keeps a miss actionable for 72 h; the coach says it once, never a make-up schedule.
- **No deload wording on cards**; deload/recovery calls cite only facts printed in the evidence block.
- **Tesamorelin 1 mg = 10u**; **BPC-157 0.5 mg Sep 1–28**.

## 4. Erik's open actions (not code)

- **Enable push notifications**: Chrome/Safari on iPhone → Share → Add to Home Screen → open from the
  icon → app Settings → Notifications → Turn on → Allow. Every push wired today is silent until then.
- Late-September lab draw (Sep 28 reminder; mark Done on the card).

## 5. Watch-list for the next few days

Today added a lot of live behaviour at once. The first week is where anything wrong will show:
chase/compliance pushes, the coach quoting max HR / dose taken-state / tape deltas / Z2 pace trend,
the nightly escalation events, the 45 s SSE idle guard, the isolation reconcile (logging an accessory
heavier once makes that the plan — un-check the set to revert). A card that disagrees with the coach
is the fastest signal; audit the coach's inputs with the no-LLM endpoints
(`/api/debug/today-status?email=`, `serve-as-user`, `coach-context`) using the READ key.

## 6. Cost rules (unchanged, non-negotiable)

No paid workflow or multi-agent run without an explicit per-run dollar approval from Erik. The
2026-08-28 run burned ≈$7,000 for zero deliverable; its output was salvaged for ~$0 and closed
today from the main loop. Bulk model calls use the Batch API. No LLM poll loops.

Suite at handoff: 1017 Python + 39 JS tests, green, zero warnings. Prod: `one2weeks-9ewf.onrender.com`, healthy.
