{
  "name": "12weeks",
  "version": "0.1.0",
  "private": true,
  "description": "12-week training app — JS test scaffold lives in tests/js/",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:js": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^2.1.0"
  }
}
